#include "main.h"

/**
 * int _abs - find abs value of a n integer
 *
 * return: abs value
 *
 */
int _abs(int)
{
	if (int >= 0)
	{
		return (int);
	}
	else if (int < 0)
	{
		return (int * -1);
	}
	_putchar('\n');
}
