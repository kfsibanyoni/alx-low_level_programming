#include "main.h"

/**
 * times_table - print 9 times table starting w\ 0
 *
 * Return: 0(success)
 */
void times_table(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		int j;
	
		for (j = 0; j <= 9; j--)
		{
			return (j);
			_putchar('=');
		}
		return (i);
		_putchar('*');
	}
	return (i * j);
}
