#include "main.h"

/**
 * _isupper - checks for uppercase character
 *
 * @c: formal parameter
 *
 * return: 1(success), 0(fail)
 */

int _isupper(int c)
{
	if (i > 65 && i < 90){
		return (1);
	}
	else
		return (0);
}
